using AZUS_Transport.Models;
using AZUS_Transport.MyUserControl;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.Linq;
using System.Linq.Expressions;
using System.Threading.Tasks;

namespace ClassLibraryTests
{



        //private ApplicationAgreedView[] products = {
        //    new ApplicationAgreedView {DispatchersStatusDone = "Согласовано", DirectorStatusDone = "На рассмотрении",
        //    EconomistStatusDone = "На рассмотрении", DepartmentStatusDone = "На рассмотрении"},
        //      new ApplicationAgreedView {DispatchersStatusDone = "На рассмотрении", DirectorStatusDone = "На рассмотрении",
        //    EconomistStatusDone = "На рассмотрении", DepartmentStatusDone = "На рассмотрении"},
        //      new ApplicationAgreedView {DispatchersStatusDone = "Согласовано", DirectorStatusDone = "Согласовано",
        //    EconomistStatusDone = "На рассмотрении", DepartmentStatusDone = "На рассмотрении"},
        //     new ApplicationAgreedView {DispatchersStatusDone = "Согласовано", DirectorStatusDone = "Согласовано",
        //    EconomistStatusDone = "Согласовано", DepartmentStatusDone = "Согласовано"},
        //};


        //[TestMethod]
        //public void Sum_Products_Correctly()
        //{
        //    var goalTotal = 1;
        //    var result = 1;

        //    //var goalTotal = products.Where(x => x.DispatchersStatusDone == "Согласовано" ||
        //    //             x.DirectorStatusDone == "Отклонена" || x.EconomistStatusDone == "Отклонена" || x.DispatchersStatusDone == "Отклонена" || x.DepartmentStatusDone == "Отклонена");

        //    //// Arrange
        //    //var discounter = new MinimumDiscountHelper();

        //    var target = new MainUserControl();


        //    // Act


        //    //var result = products.Where(x => x.DispatchersStatusDone == "Согласовано" ||
        //    //             x.DirectorStatusDone == "Отклонена" || x.EconomistStatusDone == "Отклонена" || x.DispatchersStatusDone == "Отклонена" || x.DepartmentStatusDone == "Отклонена"); ;

        //    // Assert
        //    Assert.AreEqual(goalTotal, result);
        //}



        //[TestMethod]
        //public async Task GetFirmSectionListAsync_Count_AreEqual()
        //{
        //    Expression<Func<string, int>> rootExpression = s => s.Substring(4).Length;

        //    var subExpression = ((MemberExpression)rootExpression.Body).Expression;   // just the `s.Substring(4)` part
        //    Assert.AreEqual("t string", InvokeSubExpression<string>(rootExpression, subExpression, "input string"));


        //    //var fICount = _App..ToList().Count;
        //    //var fIResult = await _firmService.GetFISListAsync(new CancellationToken(), Startup.ConnString);

        //    //Assert.AreEqual(fICount, fIResult.Entities.Count);
        //}

    }
}
